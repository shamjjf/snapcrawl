# ── Dependencies ────────────────────────────────────────────────
# Single lockfile at root is tracked; installed modules are not.
node_modules/

# ── Environment / secrets (C-05: no secrets in version control) ──
.env
.env.*
!.env.example

# ── Build outputs ───────────────────────────────────────────────
# dist/ = apps/extension (CRXJS/Vite); .next/ + out/ = apps/web (Next.js)
dist/
.next/
out/
build/
*.tsbuildinfo
# Auto-generated by Next.js on build
next-env.d.ts

# ── Local object storage ────────────────────────────────────────
# MinIO runs as a native binary here (no Docker on this box); the server and
# its bucket data are local dev artifacts. docker-compose.yml remains the
# documented/CI path. See docs/RUNNING.md.
.minio/

# ── Test output ─────────────────────────────────────────────────
# Playwright run artefacts (traces, screenshots, videos) — regenerated per run.
test-results/
playwright-report/
.playwright/

# ── Caches ──────────────────────────────────────────────────────
.vite/
.turbo/
.cache/
coverage/
.vitest/

# ── Logs ────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS / editor ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.swp
*~
